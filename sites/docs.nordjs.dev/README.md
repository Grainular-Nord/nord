# docs.nordjs.dev

Nørd's documentation site, statically generated by Aurora.

To install dependencies:

```bash
bun install
```

To start the development server:

```bash
bun run dev
```

Create a production build with `bun run build` and serve it locally with `bun run preview`.
