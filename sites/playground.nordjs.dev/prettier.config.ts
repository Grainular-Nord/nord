import type { Config } from 'prettier';

export default {
    arrowParens: 'always',
    embeddedLanguageFormatting: 'auto',
    printWidth: 88,
    semi: true,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'all',
} satisfies Config;
